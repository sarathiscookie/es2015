const fun = () => {
	const data = 'arrow function';
	return data;
}

console.log(fun()); // Not have to write anonymous functions. Instead, we can use arrow function

/*Another Method*/
const shortForm = () => 'Arrow Function';

console.log(shortForm());